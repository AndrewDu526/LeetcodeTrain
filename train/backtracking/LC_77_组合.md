# LC 77 组合

## 1. Basic Information

- __level__: medium
- __time-cost__: 未计时
- __completed-date__: 12-28-2025
- __completed-times__: 1
- __status__: 刚开始做回溯不会，后来秒了

## 2. Solution

经典回溯，注意用startindex控制。

## 3. Code Implementation

    class Solution {
    
        List<List<Integer>> result = new ArrayList<>();
        List<Integer> path = new ArrayList<>(); // LinkedList<>()
    
        public List<List<Integer>> combine(int n, int k) {
    
            backtracking(n, k, 1);
            return result;
        }
    
        public void backtracking(int n, int k, int index){
            if(path.size()==k){
                result.add(new ArrayList<Integer>(path));
                return;
            }
    
            for(int i=index;i<=n;i++){
                path.add(i);
                backtracking(n,k,i+1);
                path.remove(path.size()-1);
            }
        }
    }

## 4. Notes

- EZ

# LC 704 二分查找

## 1. Basic Information

- __level__: easy
- __time-cost__: 6 min
- __completed-date__: 1-15-2026
- __completed-times__: 1
- __status__: 独立完成

## 2. Solution

二分查找

## 3. Code Implementation

    class Solution {
        public int search(int[] nums, int target) {
            return find(nums, target, 0, nums.length-1);
        }
    
        private int find(int[] nums, int target, int left, int right){
            if(left > right) return -1;
            int index = (right + left)/2;
            if(nums[index] == target) return index;
            else if(nums[index] > target) return find(nums, target, left, index-1);
            else return find(nums, target, index+1, right);
        }
    }

## 4. Notes

- 取中间坐标需要 （上限+下限）/2。
- 递归时需要控制左右区间缩小，对mid进行增减。

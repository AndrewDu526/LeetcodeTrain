# LC 15 三数之和

## 1. Basic Information

- __question__: 三数之和
- __topic__: hash
- __level__: medium
- __time-cost__: null
- __completed-date__: 12-24-2025
- __completed-times__: 1
- __status__: 参考思路和代码，在GPT纠错下完成

## 2. Question

给你一个整数数组 nums ，判断是否存在三元组 [nums[i], nums[j], nums[k]] 满足 i != j、i != k 且 j != k ，同时还满足 nums[i] + nums[j] + nums[k] == 0 。请你返回所有和为 0 且不重复的三元组。
注意：答案中不可以包含重复的三元组。

示例 1：
输入：nums = [-1,0,1,2,-1,-4]
输出：[[-1,-1,2],[-1,0,1]]
解释：
nums[0] + nums[1] + nums[2] = (-1) + 0 + 1 = 0 。
nums[1] + nums[2] + nums[4] = 0 + 1 + (-1) = 0 。
nums[0] + nums[3] + nums[4] = (-1) + 2 + (-1) = 0 。
不同的三元组是 [-1,0,1] 和 [-1,-1,2] 。
注意，输出的顺序和三元组的顺序并不重要。
示例 2：
输入：nums = [0,1,1]
输出：[]
解释：唯一可能的三元组和不为 0 。
示例 3：
输入：nums = [0,0,0]
输出：[[0,0,0]]
解释：唯一可能的三元组和为 0 。

提示：
3 <= nums.length <= 3000
-105 <= nums[i] <= 105

## 3. Solution

注意到排序不影响答案，同时关注大小关系。因此使用双指针，先排序数组，再用for循环固定一个数值，然后用双指针控制一左一右逐渐逼近总和为0。逼近过程中对三个数值分别去重。

## 4. Code Implementation

我的答案（错误1.0）：

    class Solution {
        public List<List<Integer>> threeSum(int[] nums) {
            
            List<List<Integer>> result = new ArrayList();
            Array.sort(nums);
            
            for(int i = 0; i<nums.length(); i++){
                int j = i + 1;
                int k = nums.length()-1;
    
                //i去重
                if(i>0 && nums[i-1]==nums[i]){continue;}
    
                while(j<k){
                    if(nums[i]+nums[j]+nums[k]>0){
                        k--;
                    }else if(nums[i]+nums[j]+nums[k]<0){
                        j++;
                    }else{
                        result.add(new int[i,j,k]) 
                        if(j>0 && nums[j-1]==nums[j]){continue;}
                        if(k>0 && nums[k-1]==nums[k]){continue;}
                    }
                }
            }
        }
    }

- List<List<Integer>> result = new ArrayList<>(); 泛型未写全，缺少<>
- import java.util.Arrays; Arrays拼写错误
- nums.length;数组的length为属性而非方法不需要括号
- 忘记 i 的剪枝，由于按顺序排序 i>0 则直接break
- 通过Arrays.asList(nums[i], nums[j], nums[k])来构建数组，同时输入的是元素而下标
- 忘记在while循环中增加j, k，导致while无限循环
- j,k的去重条件应该为j<k因为while循环范围是j<k
- j,k的去重策略应该用while(){k++}和while(){j++}

我的答案（错误2.0）：

    import java.util.Arrays;
    
    class Solution {
        public List<List<Integer>> threeSum(int[] nums) {
            
            List<List<Integer>> record = new ArrayList<>();
            Arrays.sort(nums);
    
            for(int i=0;i<nums.length-1;i++){
    
                if(nums[i]>0){break;}
                
                while(i>0 && nums[i]==nums[i-1]){i++;}
    
                int j = i+1;
                int k = nums.length-1;
    
                while(j<k){
                    if(nums[i]+nums[j]+nums[k]>0){
                        k--;
                    }else if(nums[i]+nums[j]+nums[k]<0){
                        j++;
                    }else{
                        record.add(Arrays.asList(nums[i], nums[j], nums[k]));
                    }
    
                    while (k > j && nums[k] == nums[k - 1]) k--;
                    while (k > j && nums[j] == nums[j + 1]) j++;
                    
                    k--; 
                    j++;
                }
            }
            return record;
        }
    }

- i的去重策略是continue，因为在for循环中已经增加i了，不需要while来自增。
- j,k的去重和自增应该位于else语句中。

标准答案：
  
    import java.util.Arrays;
    
    class Solution {
        public List<List<Integer>> threeSum(int[] nums) {
            
            List<List<Integer>> record = new ArrayList<>();
            Arrays.sort(nums);
    
            for(int i=0;i<nums.length-1;i++){
    
                if(nums[i]>0){break;}
                
                if(i>0 && nums[i]==nums[i-1]){continue;}
    
                int j = i+1;
                int k = nums.length-1;
    
                while(j<k){
                    if(nums[i]+nums[j]+nums[k]>0){
                        k--;
                    }else if(nums[i]+nums[j]+nums[k]<0){
                        j++;
                    }else{
                        record.add(Arrays.asList(nums[i], nums[j], nums[k]));
                        while (k > j && nums[k] == nums[k - 1]) k--;
                        while (k > j && nums[j] == nums[j + 1]) j++;
                    
                        k--; 
                        j++;
                    }
                }
            }
            return record;
        }
    }

## 5. Conclusion
- 排序，双指针和去重，此类题目不熟练，先死记硬背题解。
- 复习ArrayList类内容。


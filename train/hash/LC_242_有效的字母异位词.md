# LC 242 有效的字母异位词

## 1. Basic Information

- __question__: 有效的字母异位词
- __topic__: hash
- __level__: easy
- __time-cost__: null
- __completed-date__: 12_22_2025
- __completed-times__: 1
- __status__: fail to solve independently

## 2. Question

给定两个字符串 s 和 t ，编写一个函数来判断 t 是否是 s 的字母异位词。

示例 1:
输入: s = "anagram", t = "nagaram"
输出: true
示例 2:
输入: s = "rat", t = "car"
输出: false

提示:
1 <= s.length, t.length <= 5 * 104
s 和 t 仅包含小写字母

## 3. Solution

use hash array to record the first array's elements: each element maps to a 26-digits length array, increase its value. Then map the second array to the same hashing array, but decrease this time. Finally loop the hashing array to check whether all elements are zero.

## 4. Code Implementation

    class Solution {
        public boolean isAnagram(String s, String t) {
            
            if(s.length() != t.length()){
                return false;
            }
    
            int[] hash = new int [26];
    
            for(char c : s.toCharArray()){
                hash[c-'a']++;
            }
    
            for(char c : t.toCharArray()){
                hash[c-'a']--;
            }
    
            for(int i : hash){
                if(i != 0){return false;}
            }
    
            return true;
        }
    }


---
## 5. Conclusion
- fail to construct and initialize an array: `int hash = new int[26]`
- fail to transfer a string to a character array: `s.toCharArray()`
- learn more about hash
- learn more about java hashmap, hashset data structure 
---

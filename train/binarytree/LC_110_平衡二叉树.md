# LC 110 平衡二叉树

## 1. Basic Information

- __level__: easy
- __time-cost__: 未计时
- __completed-date__: 1-3-2026
- __completed-times__: 1
- __status__: 在GPT纠错下完成

## 2. Solution

先完成计算高度的函数，然后递归所有节点比较左右子节点的高度差绝对值是否小于1

## 3. Code Implementation

    class Solution {
        public boolean isBalanced(TreeNode root) {
            if(root != null){
                int diff = height(root.left) - height(root.right);
                if(Math.abs(diff) <= 1) return isBalanced(root.left) && isBalanced(root.right);
                else return false;
            }
    
            return true;
        }
    
        private int height(TreeNode node){
            if(node == null){return 0;}
            return 1 + Math.max(height(node.left), height(node.right));
        }
    }

## 4. Notes

- 将比较 和 计算高度 两个方法分开
- 不理解“高度”的含义
- 了解Math.abs()
- 不理解“平衡二叉树”的概念
- 注意高度差值的代码实现
- 难度有中等

# LC 257 二叉树的所有路径.md

## 1. Basic Information

- __level__: easy
- __time-cost__: 超时
- __completed-date__: 1-3-2026
- __completed-times__: 1
- __status__: 对照代码纠错完成

## 2. Solution

回溯，维护一个result和单层，if检测节点并组合一条路径。

## 3. Code Implementation

我的错误答案：

    class Solution {
    
        List<String> result = new ArrayList<>();
        StringBuilder sb = new StringBuilder();
    
        public List<String> binaryTreePaths(TreeNode root) {
            if(root == null){return result;}
            backtracking(root);
            return result;
        }
    
        private void backtracking(TreeNode node){
    
            sb.append(node.val);
            if(node.left != null || node.right != null){sb.append("->");}
    
            if(node.left != null){
                backtracking(node.left);
                sb.deleteCharAt(sb.length()-1);
                sb.deleteCharAt(sb.length()-1);
                sb.deleteCharAt(sb.length()-1);
            }else{
                result.add(sb.toString());
                return;
            }
            
            if(node.right != null){
                backtracking(node.right);
                sb.deleteCharAt(sb.length()-1);
                sb.deleteCharAt(sb.length()-1);
                sb.deleteCharAt(sb.length()-1);
            }else{
                result.add(sb.toString());
                return;
            }
        }
    }

正确答案：

class Solution {
    /**
     * 递归法
     */
    public List<String> binaryTreePaths(TreeNode root) {
        List<String> res = new ArrayList<>();// 存最终的结果
        if (root == null) {
            return res;
        }
        List<Integer> paths = new ArrayList<>();// 作为结果中的路径
        traversal(root, paths, res);
        return res;
    }

    private void traversal(TreeNode root, List<Integer> paths, List<String> res) {
        paths.add(root.val);// 前序遍历，中
        // 遇到叶子结点
        if (root.left == null && root.right == null) {
            // 输出
            StringBuilder sb = new StringBuilder();// StringBuilder用来拼接字符串，速度更快
            for (int i = 0; i < paths.size() - 1; i++) {
                sb.append(paths.get(i)).append("->");
            }
            sb.append(paths.get(paths.size() - 1));// 记录最后一个节点
            res.add(sb.toString());// 收集一个路径
            return;
        }
        // 递归和回溯是同时进行，所以要放在同一个花括号里
        if (root.left != null) { // 左
            traversal(root.left, paths, res);
            paths.remove(paths.size() - 1);// 回溯
        }
        if (root.right != null) { // 右
            traversal(root.right, paths, res);
            paths.remove(paths.size() - 1);// 回溯
        }
    }
}

## 4. Notes

- 我理解回溯的思路，然而这里我尝试直接操作字符串并且要处理->，因此操作尤其是回溯混乱有误。实际应该操作一个列表，装载元素。
- 难度应该为中
- 二刷尝试重写此题
- 尝试另一个更简洁的方法

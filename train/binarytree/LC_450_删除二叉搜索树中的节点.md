# LC 450 删除二叉搜索树中的节点

## 1. Basic Information

- __level__: medium
- __time-cost__: 未计时
- __completed-date__: 1-11-2026
- __completed-times__: 1
- __status__: 看代码后完成

## 2. Solution

只有第五种情况，删除的节点同时拥有左右节点时，需要重构二叉树，即将计划删除的节点的左树拼接到右树的最左叶节点，因此需要循环获得左叶节点。

## 3. Code Implementation

    class Solution {
        public TreeNode deleteNode(TreeNode root, int key) {
            if(root == null) return null;
            if(root.val == key){
                if(root.left == null && root.right == null){
                    return null;
                }else if(root.left == null && root.right != null){
                    return root.right;
                }else if(root.left != null && root.right == null){
                    return root.left;
                }else{
                    TreeNode cur = root.right;
                    while(cur.left != null){
                        cur = cur.left;
                    }
                    cur.left = root.left;
                    return root.right;
                }
            }
    
            if (root.val > key) root.left = deleteNode(root.left, key);
            if (root.val < key) root.right = deleteNode(root.right, key);
    
            return root;
        }
    }

## 4. Notes

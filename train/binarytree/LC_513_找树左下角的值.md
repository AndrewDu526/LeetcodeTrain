# LC 513 找树左下角的值

## 1. Basic Information

- __level__: medium
- __time-cost__: 10 min
- __completed-date__: 1-3-2026
- __completed-times__: 1
- __status__: 秒了

## 2. Solution

层序遍历后最后一层的第一个元素

## 3. Code Implementation

    class Solution {
        List<List<Integer>> result = new ArrayList<>();
        List<Integer> layer = new ArrayList<>();
    
        public int findBottomLeftValue(TreeNode root) {
            search(root, 0);
            return result.get(result.size()-1).get(0);
        }
    
        private void search(TreeNode node, int deep){
            if(node == null){return;}
            deep++;
            if(result.size()<deep){result.add(new ArrayList<>());}
            result.get(deep-1).add(node.val);
            search(node.left, deep);
            search(node.right, deep);
        }
    }

## 4. Notes

- 寻找更好的方法

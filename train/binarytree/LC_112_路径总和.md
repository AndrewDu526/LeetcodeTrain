# LC 112 路径总和

## 1. Basic Information

- __level__: easy
- __time-cost__: 超时
- __completed-date__: 1-3-2026
- __completed-times__: 1
- __status__: 在GPT纠错下完成

## 2. Solution

递归，检测当前节点是否为子节点，若是则检查路径和；若不是子节点，递归向下。

## 3. Code Implementation

    class Solution {
        public boolean hasPathSum(TreeNode root, int targetSum) {
            if(root == null){
                return false;
            }
    
            return check(root, targetSum);
        }
    
        private boolean check(TreeNode root, int targetSum){
            targetSum -= root.val;
            if(root.left == null && root.right == null && targetSum == 0){
                return true;
            }else if(root.left != null || root.right != null){
                return hasPathSum(root.left, targetSum) || hasPathSum(root.right, targetSum);
            }
    
            return false;
        }
    }

## 4. Notes

- 使用递归完成，有些复杂；尝试遍历回溯，我认为会更简单，虽然是笨办法。
- 难度有中。
- 注意非子节点递归向下时，左右路径不需要全为true，只要有一个的路径和等于目标值就成功，因此用||。
- 使用递归答题速度超时，第一次。
